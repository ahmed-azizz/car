import 'package:flutter/material.dart';

class ford_view extends StatelessWidget {
  const ford_view({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}