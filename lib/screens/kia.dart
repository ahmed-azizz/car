import 'package:flutter/material.dart';

class kia_view extends StatelessWidget {
  const kia_view({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}