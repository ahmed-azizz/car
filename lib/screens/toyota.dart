import 'package:flutter/material.dart';

class toyota_view extends StatelessWidget {
  const toyota_view({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}